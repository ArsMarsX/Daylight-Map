package com.revolve44.drawmap3;

import androidx.appcompat.app.AppCompatActivity;

import android.graphics.Color;
import android.graphics.PorterDuff;
import android.graphics.drawable.Drawable;
import android.os.Bundle;
import android.widget.FrameLayout;
import android.widget.ImageView;

public class MainActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);
        //ImageView cat = findViewById(R.id.beercat);
        //cat.setColorFilter(Color.rgb(0, 123, 123), android.graphics.PorterDuff.Mode.MULTIPLY);
        //Drawable.setColorFilter( 0xffff0000, PorterDuff.Mode.MULTIPLY );

        //ImageView lineColorCode = (ImageView)convertView.findViewById(R.id.line_color_code);

//        frameLayout = (FrameLayout) findViewById(R.id.over);
//        frameLayout.removeAllViews();
//        frameLayout.addView(tutorialView);
    }
}
